// import { TestingModule, Test } from '@nestjs/testing';
// import {
//   STATSD_INTERCEPTOR_OPTIONS_PROVDER,
//   StatsDInterceptor,
// } from './statsd.interceptor.ts.interceptor';
// import { StatsDInterceptorModule } from './nestjs-statsd-interceptor.module';
// import { APP_INTERCEPTOR } from '@nestjs/core';

// let appInterceptor: StatsDInterceptor;

describe('Statsd.Interceptor.TsInterceptor', () => {
  beforeEach(async () => {
    // const module: TestingModule = await Test.createTestingModule({
    //   imports: [StatsDInterceptorModule.configure()],
    // }).compile();
    // appInterceptor = module.get(APP_INTERCEPTOR);
  });

  it('should register to as app interceptor', async () => {
    // const module: TestingModule = await Test.createTestingModule({
    //   imports: [StatsDInterceptorModule.configure()],
    // }).compile();
    // expect(appInterceptor).toBeDefined();
  });
});
